# include <stdio.h>

int main(void)
{
    int a = 11, x;

    if (a == 69) {
        printf("I is %d", a);
    }

    else {
        printf("a is not 6. a is %d\n", a);
    }

    x = 0;

    while (x < 10){
        printf("x is %d now\n", x);
        x++;
    }
    printf("All done!\n");

    // y += x > 10? 17: 37;

    return 0;
}
